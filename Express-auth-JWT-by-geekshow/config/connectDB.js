const mongoose = require("mongoose")

const conncetDb = async (DATABASE_URL) => {
    try {
        const DB_OPTIONS = {
            db: "geekshop"
        }
        
    } catch (error) {
        
    }
}